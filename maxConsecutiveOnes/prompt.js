// Write a function that takes in an array of numbers and returns the maximum number of consecutive 1s

//maxConsecutiveOnes([1,1,2,0,3,1,1,1,1,5,6,3]) returns 4


// Return numbers that increase by one consecutively.
// Output is the total amount of consecutive numbers.
// Iterate through array.
// Check if the numbers in array are consecutive.
// 
function maxConsecutiveOnes(array) {
  let max = 0;
  let count = 0;
  let start = array[0];
  let arrLength = array.length;
  
    for (let i = 0; i < arrLength - 1; ++i) {
      if (start + 1 ) {
         start + 1;
      }
    }
    return max;
  }
   //write solution code